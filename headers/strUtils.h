#ifndef strUtils_H
#define strUtils_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>
#include <stdint.h>

// Verifica se o endereço inserido esta dentro do padrão de endereço.
int verificaIp (char *ip);

#endif